# yt-dlp[default] zieht yt-dlp-ejs mit, das die JS-Signature-Solver bringt.
# Nightlies haben Support fuer die aktuelle YouTube-Challenge -> --pre im Dockerfile.
# Deno als JS-Runtime ebenfalls im Dockerfile installiert.
flask==3.1.3
yt-dlp[default]>=2026.5.0
gunicorn==23.0.0
# IANA-Zeitzonen fuer zoneinfo (glappa-chat: Datum/Uhrzeit im System-Prompt).
# Noetig auf Systemen ohne System-tzdata (Windows-Dev, manche Slim-Images).
tzdata
# GeoIP-Reader (Link-Kuerzer-Statistik: IP -> Laendercode, offline).
# Die Datenbank selbst (DB-IP Country Lite) laedt der Dockerfile-Build.
maxminddb
# Datei-Konverter (home/convert.html): Bilder ueber Pillow, PDF-Seiten <->
# Bild ueber PyMuPDF. Beides reine Wheels, kein zusaetzliches apt-Paket -
# Audio/Video laeuft ueber das schon vorhandene ffmpeg-Binary.
Pillow>=10.0
pymupdf>=1.24
